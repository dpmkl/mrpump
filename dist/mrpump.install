post_install() {
  if ! getent passwd mrpump >/dev/null; then
    useradd -r -g mrpump -d /var/lib/mrpump -s /usr/bin/nologin mrpump
  fi
  mkdir -p /var/lib/mrpump
  chown mrpump:mrpump /var/lib/mrpump
}

post_upgrade() {
  post_install
}
